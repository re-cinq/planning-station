import { type BlockJson } from "../blocks/block-json.js";
import type { AgentOp } from "./agent-ops.js";
/** Applies the planning agent's edits to a plan's blocks. */
export declare function applyOps(blocks: readonly BlockJson[], ops: readonly AgentOp[]): BlockJson[];
//# sourceMappingURL=apply-ops.d.ts.map