import { PropFieldProps } from './PropField.js';
/** A prototype matures in order, so its maturity reads as steps: the ones behind it are reached. */
export declare function MaturitySteps({ spec, value, onChange, readOnly, }: PropFieldProps): import("react").JSX.Element;
