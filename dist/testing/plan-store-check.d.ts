import type { PlanStore } from "../ports/plan-store.js";
/** Names everything a PlanStore got wrong, so a host can check its own one. */
export declare function checkPlanStore(store: PlanStore): Promise<string[]>;
//# sourceMappingURL=plan-store-check.d.ts.map